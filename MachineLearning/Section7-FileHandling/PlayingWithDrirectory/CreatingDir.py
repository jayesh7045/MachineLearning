import os
#os.makedirs("package_create_using_os")

# It will give error, if the directory with the same name already exists
print(os.listdir('.')) #To look out for all the dirs and files present in the particular folder
